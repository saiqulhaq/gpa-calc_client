import {
  calculateQp
} from '../../../helpers/calculate-qp';
import { module, test } from 'qunit';

module('CalculateQpHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = calculateQp(42);
  assert.ok(result);
});
